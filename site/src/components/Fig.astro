---
/** A reference to a figure by its id; the number is filled in by the page, in the order the captions count. */
interface Props {
    id: string;
}
const { id } = Astro.props;
---

<a class="figref" href={`#${id}`}>figure</a>
